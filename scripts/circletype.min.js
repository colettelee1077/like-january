import CircleType from `circletype`;

const archUp = new CircleType(document.getElementById('curve-up'));
archUp.radius(384);

const archDown = new CircleType(document.getElementById('curve-down'));
archDown.dir(-1);
archDown.radius(384);
